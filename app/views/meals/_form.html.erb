<%= form_for(@meal) do |f| %>
  <% if @meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% @meal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wine_id %><br />
    <%= f.number_field :wine_id %>
  </div>
  <div class="field">
    <%= f.label :bread_id %><br />
    <%= f.number_field :bread_id %>
  </div>
  <div class="field">
    <%= f.label :cheese_id %><br />
    <%= f.number_field :cheese_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
