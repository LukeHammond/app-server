<%= form_for(@bread, :url => bread_path) do |f| %>
  <% if @bread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bread.errors.count, "error") %> prohibited this bread from being saved:</h2>

      <ul>
      <% @bread.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
